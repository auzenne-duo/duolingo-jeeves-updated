version: "2"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    tty: true
    image: duolingojeeves_api
    ports:
      - "5000:5000"
    volumes:
      - .:/code
      - ~/.aws:/root/.aws
