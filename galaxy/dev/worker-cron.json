[
  {
    "command": [
      "python",
      "./jeeves/scripts/index_pipeline_and_spike_detector/slack.py"
    ],
    "cpu": ${cpu},
    "environment": ${environment_vars},
    "essential": true,
    "image": "${ecr_repo}:${version}",
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${service}",
        "awslogs-region": "${region}",
        "awslogs-stream-prefix": "${service}",
        "mode": "non-blocking"
      }
    },
    "memory": ${memory},
    "name": "${service}",
    "secrets": ${secrets},
    "ulimits": [
      {
        "hardLimit": 65535,
        "name": "nofile",
        "softLimit": 65535
      }
    ]
  }
]
